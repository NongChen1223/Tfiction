# Tfiction 开发进度汇报

## 时间：2026-03-13

## 本次提交的改进

### 0. 整版风格统一继续推进

- ✅ 新增项目内 `Toggle` / `ColorField` 通用控件，后续其他页面也可以直接复用
- ✅ 阅读设置页去掉 `antd` 的 `Select / Slider / ColorPicker / Switch` 外壳，统一切到项目内硬边控件
- ✅ “添加文件到目录”弹窗去掉默认 `antd Modal / Checkbox`，改成项目内 `Dialog` + 主题化选择列表
- ✅ 首页书籍/目录重命名去掉系统 `prompt`，改成项目内重命名弹窗，支持空值校验和点击空白取消
- ✅ 书架卡片删除按钮补上独立点击链路，避免被卡片打开行为吞掉，删除确认弹窗可稳定触发
- ✅ 导入文件成功后不再强制进入阅读页，只刷新当前书架或目录
- ✅ 导入弹窗选项卡片扩大为整块可点击，不再只有中间图标容易触发
- ✅ 设置页左侧切换分类时，右侧内容区会自动回到顶部，避免沿用上一个分类的滚动位置
- ✅ 夜间主题主色和阴影改成更柔和的夜间靛紫，降低刺眼感
- ✅ 书籍封面底部标题区改成主题感知渐变，白天/护眼模式不再固定压一层黑
- ✅ 阅读模式工具栏、目录面板、搜索面板先去掉杂乱图标，统一改成纯文字按钮
- ✅ `Tabs` 和 `Slider` 的底层样式变量继续收口，避免暗色主题下出现旧变量和旧阴影混用
- ✅ 补齐字体尺寸变量，避免部分弹窗和卡片使用未定义字号变量

### 0.1 本次验证结果

```bash
cd frontend && npm run type-check
cd frontend && npm run build
GOCACHE=$(pwd)/.gocache go test ./...
```

结果：
- 前端类型检查通过
- 前端生产构建通过
- Go 测试通过

### 0.2 阅读摸鱼模式透明化修复

- ✅ 修复阅读页调整摸鱼可见度后返回书架仍继续生效的问题，透明状态现在只作用于阅读页
- ✅ 阅读页摸鱼模式改成透明背景方案，不再整体压暗页面，默认仅保留正文和必要控件
- ✅ 离开阅读页或返回书架时会主动清理摸鱼状态，避免书架页和设置页继承透明样式
- ✅ 老板模式相关文案同步改成“文字可见度”，设置页预览也改成透明背景语义
- ✅ 阅读页摸鱼模式继续清掉残留边框、阴影和底色，最低文字可见度下更接近“文字悬浮”效果
- ✅ Wails 原生窗口背景色改成透明，避免前端已经透明时外层窗口仍然像一整块深色面板
- ✅ 已放弃“整窗透明/无框主窗口”路线，主窗口恢复普通行为，不再影响正常阅读与缩放
- ✅ macOS 阅读摸鱼模式继续使用原生桌面歌词浮窗：进入后隐藏主窗口，但上下章、目录、底部进度条和文字透明度控件都直接放回浮窗内部
- ✅ 原生浮窗支持双击文本区或按 `Esc` 退出，退出后会把主阅读窗口重新带回前台
- ✅ 浮窗内容会跟随章节、字体大小、行高和文字可见度实时同步，不再只是给当前主窗口做一层透明效果
- ✅ 桌面浮窗首次打开会贴近主阅读窗口中心，不再固定在一组生硬坐标上“跳到屏幕别处”
- ✅ 原生浮窗补上轻量拖拽框、顶部拖动提示和右下角尺寸手柄，低可见度下仍能定位、拖拽和缩放
- ✅ 用户拖拽后的浮窗位置会被复用；如果显示器变化导致浮窗跑偏，重新进入时会自动收回可见区域
- ✅ 浮窗边框改为“只在窗口内侧边框带显示”，正文内圈和窗口外侧都不会误触发，悬浮阅读时视觉更干净
- ✅ 正文区域已支持左键按住直接拖拽窗口，不再必须去找顶部拖动区才能移动
- ✅ 浮窗滚轮事件改为优先由悬浮阅读层消费，减少滚动时误穿透到底层应用的问题
- ✅ 浮窗原生控件通过动作队列回传给前端阅读逻辑，避免按钮点击直接打进 Wails 原生主线程再次引发崩溃

### 1. 目录页头部与首页搜索框细化

- ✅ 二级目录头部改为“箭头 + 书架 / 当前目录”单行结构，去掉冗余文案
- ✅ 目录头部进一步上提，并和下面封面区域拉开更舒服的间距
- ✅ 返回箭头左对齐到书籍内容起点，避免视觉错位
- ✅ 首页搜索框去掉发散感和模糊效果，仅保留纯色边框线

### 2. 阅读入口稳定性补强

- ✅ `OpenNovel` 调用改为通过桥接层重试官方 Wails service wrapper
- ✅ 避免目录内点击阅读时直接命中 `window.go.services` 注入竞态

### 3. EPUB 封面读取补齐

- ✅ 后端 `EPUB` 解析已接入 metadata / manifest / guide 的封面定位
- ✅ 支持从 `cover.xhtml`、`titlepage.xhtml`、SVG `<image>`、样式 `background-image` 中继续追到真实封面图
- ✅ 书籍自带封面时会转成前端可直接显示的 data URL
- ✅ 顶层书架和目录内文件都会同步保留封面数据
- ✅ 没有封面的书籍仍然保持默认图标回退

### 4. 本次验证结果

```bash
cd frontend && npm run type-check
cd frontend && npm run build
GOCACHE=$(pwd)/.gocache go test ./...
```

结果：
- 前端类型检查通过
- 前端生产构建通过
- Go 测试通过

### 5. 删除确认交互补齐

- ✅ 删除目录时会明确提示“确定删除此目录吗？”并显示目录名
- ✅ 删除书籍时会自动带上书名和文件后缀再确认
- ✅ 删除完成后会立即从书架移除，并清理对应阅读进度
- ✅ 删除完成后会补一条即时成功提示，减少误触后的不确定感

### 6. 弹窗样式统一

- ✅ 新增项目内通用 `Dialog` 组件，统一遮罩、居中、Esc 关闭和按钮区域样式
- ✅ 导入文件弹窗已切到自定义 `Dialog`，不再依赖默认 antd 弹窗外壳
- ✅ 删除确认已改为项目内确认弹窗，视觉风格与导入流程保持一致
- ✅ 删除确认进一步收敛为更简约的硬边风格，去掉警告图标和大红色圆角按钮
- ✅ 删除确认的硬边弹窗已接入 `light / dark / sepia` 三套主题配色，不再使用固定死色

## 时间：2026-03-12

## 本次提交的改进

### 1. 目录浏览链路修复

- ✅ 点击目录时改为进入目录下一层，不再直接跳进阅读模式
- ✅ 目录卡片补上“进入目录 / 立即阅读 / 导入文件”三类明确操作
- ✅ 目录页顶部新增返回书架入口和当前路径显示
- ✅ 在目录页内也可继续把已有文件或新文件添加进当前目录

### 2. 阅读返回链路补齐

- ✅ 阅读页顶部新增显式返回按钮，可直接回书架
- ✅ 如果是从目录内打开文件，返回按钮会准确回到原目录
- ✅ 返回书架快捷键也会复用同一条返回逻辑，避免跳错层级

### 3. 目录持久化与脏数据修复

- ✅ 修复目录内文件在阅读进度更新后被重新塞回顶层书架的问题
- ✅ 新增书架持久化规范化处理，启动时会自动清理目录内文件的顶层重复项
- ✅ 目录文件现在保留作者、阅读进度和最近阅读时间等完整元数据
- ✅ 支持目录内文件重命名、移除，避免进入目录后只能看不能管

### 4. 本次验证结果

```bash
cd frontend && npm run type-check
cd frontend && npm run build
```

结果：
- 前端类型检查通过
- 前端生产构建通过

## 时间：2026-03-11

## 本次提交的改进

### 1. 书架从 mock 改为真实可用

- ✅ 新增持久化书架 store，首页不再依赖模拟数据
- ✅ 支持导入单文件后直接进入阅读页
- ✅ 支持创建目录、将已有单文件移动进目录
- ✅ 支持书架条目重命名、删除、最近阅读排序

### 2. 阅读主链路打通

- ✅ `OpenNovel("")` 已接入系统文件选择器
- ✅ 阅读页改为加载真实章节内容，不再依赖错误的前端切片
- ✅ 章节切换、键盘翻章、目录跳转可用
- ✅ 阅读滚动时自动保存总进度，并同步回书架卡片
- ✅ 重新打开同一本书时恢复章节和进度

### 3. 搜索与定位增强

- ✅ 全文搜索结果支持显示章节名与行号
- ✅ 点击搜索结果可跳转到对应章节位置
- ✅ 阅读内容高亮改为安全 HTML 渲染，避免关键字和正文直接拼接带来的风险

### 4. 后端稳定性修复

- ✅ 章节位置统一改为前端可安全消费的字符索引
- ✅ 修复重复章节标题导致 `StartPos/EndPos` 计算错误的问题
- ✅ 搜索结果位置与章节内容提取统一使用同一套索引规则
- ✅ `NovelService` 与 `ProgressService` 已接通，进度持久化生效
- ✅ `getCurrentTimestamp()` 已改为真实时间戳

### 5. 本次验证结果

```bash
cd frontend && npm run type-check
cd .. && GOCACHE=$(pwd)/.gocache go test ./...
cd frontend && npm run build
```

结果：
- 前端类型检查通过
- Go 测试通过
- 前端生产构建通过

### 6. BOSS 模式二次优化

- ✅ 新增基础隐身 / 完全隐身双模式
- ✅ 新增悬停唤出、移出延迟隐藏，交互更平滑
- ✅ 新增右键快捷控制面板，可直接切模式、调透明度、调显隐延迟
- ✅ `Ctrl+Shift+H` 可快速切换模式，`Esc` 可立即隐藏当前内容
- ✅ 阅读页顶部与底部控件会在隐身状态下自动淡出，减少视觉暴露

### 7. 快捷键自定义接入完成

- ✅ 新增统一快捷键配置模型与解析工具
- ✅ 设置页支持录制式修改快捷键，不再是静态展示
- ✅ 支持重复键位占用检测与一键恢复默认配置
- ✅ 阅读页已接入自定义快捷键，当前支持：
  - BOSS 模式切换
  - 快速隐藏
  - 打开搜索
  - 上一章 / 下一章
  - 返回书架

### 8. 导入到阅读链路补齐

- ✅ 导入单文件后会直接入书架并进入阅读器
- ✅ 目录快速阅读会优先打开最近阅读的子文件
- ✅ 书架卡片已支持直接以 BOSS 模式打开单本书
- ✅ 导入到目录后的文件会同步更新目录最近阅读状态
- ✅ 导入失败 / 打开失败 / 空目录等场景补上即时提示

### 9. EPUB 导入能力补齐

- ✅ 后端已接通 EPUB 解包、`container.xml` 定位和 OPF 清单解析
- ✅ 可从 spine 顺序提取 XHTML 正文并生成章节，章节位置与阅读器字符索引一致
- ✅ 遇到 spine 不规范的 EPUB 时，会退回 manifest 兜底扫描可读正文
- ✅ 已完成最小 EPUB 实导验证，能正常读取书名、章节数和正文内容

## 时间：2026-03-09

## 本次提交的改进

### 1. 后端改进

#### 修复章节解析逻辑 (`backend/services/novel_service.go`)

**问题：**
- 原来的章节解析使用行号作为位置，但存储时使用字符位置，导致内容提取错误
- 章节识别模式单一，只支持 "第X章" 格式

**改进：**
- ✅ 修复了章节位置记录，使用字符位置而非行号
- ✅ 支持多种章节标题格式：
  - `第X章` / `第X节` / `第X回`
  - `Chapter X`
  - `数字.` (如 `1. xxx`)
  - `【第X章】` / `（第X章）` / `[第X章]`
  - 中文数字（零一二三四五六七八九十百千）
- ✅ 添加阅读进度保存功能 (`SaveReadingProgress`)
- ✅ 添加设置当前章节功能 (`SetCurrentChapter`)
- ✅ 改进文件标题提取（自动去除扩展名）

**影响：**
- 能够更准确地识别各种格式的章节标题
- 正确提取章节内容
- 保存阅读进度，下次打开可以继续阅读

---

### 2. 前端改进

#### 完善阅读器组件 (`frontend/src/pages/Reader/index.tsx`)

**新增功能：**

1. **工具栏**
   - 章节导航按钮（上一章/下一章）
   - 目录按钮（展开/收起章节列表）
   - 搜索按钮（打开搜索面板）
   - 摸鱼模式按钮
   - 透明度调节滑块（摸鱼模式下显示）

2. **侧边栏**
   - 显示完整的章节列表
   - 点击章节可以跳转
   - 当前章节高亮显示
   - 可以收起/展开

3. **搜索功能**
   - 全文搜索关键字
   - 显示搜索结果数量
   - 显示匹配位置的上下文
   - 高亮显示搜索关键字
   - 支持 Ctrl+F 快捷键

4. **摸鱼模式界面**
   - 一键切换摸鱼模式
   - 调节窗口透明度
   - 鼠标悬停时显示内容
   - 平滑的透明度过渡动画

5. **章节导航**
   - 显示当前章节标题和章节号
   - 键盘快捷键支持（← 上一章，→ 下一章）
   - 工具栏按钮控制

6. **阅读进度显示**
   - 底部显示进度条
   - 显示阅读进度百分比

#### 新增样式文件 (`frontend/src/pages/Reader/Reader.module.scss`)

**样式特性：**
- 响应式布局
- 平滑的过渡动画
- 自定义滚动条样式
- 工具栏、侧边栏、搜索面板样式
- 摸鱼模式下的透明度效果
- 亮色/暗色主题支持（CSS 变量）

---

## 已完成的功能

根据 `FEATURES.md`，以下功能已经完成：

### ✅ Phase 1 - 基础功能完善

- ✅ 项目框架搭建
- ✅ TXT 格式支持
- ✅ 基础阅读界面
- ✅ 章节导航
- ✅ 全文搜索
- ✅ 摸鱼模式
- ✅ 窗口置顶

### 🔄 Phase 2 - 格式支持扩展（进行中）

- ✅ TXT 格式解析完善
- ⏳ EPUB 格式支持（待实现）
- ⏳ PDF 格式支持（待实现）
- ⏳ MOBI/AZW3 格式支持（待实现）
- ⏳ 格式转换功能（待实现）
- ⏳ 图片显示支持（待实现）

---

## 技术实现亮点

### 1. 智能章节识别

使用多个正则表达式模式识别章节标题，提高识别准确率：

```go
patterns := []string{
    `^第[0-9零一二三四五六七八九十百千]+[章节回]`,
    `^Chapter\s+\d+`,
    `^\d+\.\s+`,
    `^【第.+?】`,
    ...
}
```

### 2. 优雅的摸鱼模式

- 前端状态管理（Zustand）
- 后端事件通信（Wails Events）
- 平滑的 CSS 过渡动画
- 鼠标事件监听

### 3. 响应式设计

- 使用 CSS 变量支持主题切换
- SCSS 模块化样式
- 自适应布局
- 自定义滚动条

---

## 待解决的问题

### 1. Git 推送失败

**问题：**
```
Connection closed by 198.18.0.89 port 22
致命错误：无法读取远程仓库。
```

**可能原因：**
- SSH 连接问题
- 网络代理配置
- SSH 密钥配置

**解决方案（用户需要手动操作）：**

#### 方案 1：检查 SSH 配置
```bash
# 测试 SSH 连接
ssh -T git@github.com

# 如果失败，重新配置 SSH
ssh-keygen -t ed25519 -C "your_email@example.com"
# 然后添加到 GitHub Settings > SSH Keys
```

#### 方案 2：使用 HTTPS 推送
```bash
# 修改远程 URL 为 HTTPS
cd ~/Desktop/PersonDevelop/GO_Tfiction
git remote set-url origin https://github.com/NongChen1223/Tfiction.git

# 推送
git push origin main
```

#### 方案 3：检查网络代理
```bash
# 检查是否有代理设置
git config --global --get http.proxy
git config --global --get https.proxy

# 如果有代理，确保配置正确
# 或者取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

---

## 下一步计划

### 短期任务（高优先级）

1. **修复 Git 推送问题**
   - 配置正确的 SSH 或使用 HTTPS
   - 验证推送成功

2. **完善阅读进度保存**
   - 实现持久化存储到 `~/.tfiction/progress.json`
   - 应用启动时加载进度

3. **优化性能**
   - 实现章节内容懒加载
   - 限制缓存的小说数量
   - 虚拟滚动优化

### 中期任务（中优先级）

1. **EPUB 格式支持**
   - 使用 `go-epub` 库解析 EPUB
   - 提取章节和图片
   - 支持目录导航

2. **阅读设置优化**
   - 字体大小调节
   - 行高调节
   - 页面宽度调节
   - 预设主题切换

3. **搜索功能增强**
   - 正则表达式搜索
   - 搜索历史记录
   - 搜索结果跳转

### 长期任务（低优先级）

1. **PDF 格式支持**
   - 使用 `unidoc/unipdf` 库解析 PDF
   - 提取文本内容
   - 保留排版格式

2. **书签和标注**
   - 添加书签
   - 文本高亮
   - 笔记功能

3. **云同步（可选）**
   - 配置云存储
   - 同步阅读进度
   - 多设备支持

---

## 提交记录

```
a4cf11e feat: 完善阅读器组件，添加章节导航、搜索功能、摸鱼模式界面
91828d6 fix: 修复章节解析逻辑，支持多种章节标题格式，添加阅读进度保存功能
```

---

## 文件变更

### 修改的文件
- `backend/services/novel_service.go` - 章节解析和进度管理
- `frontend/src/pages/Reader/index.tsx` - 阅读器组件

### 新增的文件
- `frontend/src/pages/Reader/Reader.module.scss` - 阅读器样式

### 待推送到 GitHub 的提交
- 2 个新提交需要推送

---

## 总结

本次开发主要完成了：
1. ✅ 修复了章节解析逻辑的严重 bug
2. ✅ 完善了阅读器界面和交互
3. ✅ 添加了章节导航、搜索、摸鱼模式等核心功能
4. ✅ 实现了优雅的 UI 和样式
5. ⏳ Git 推送需要用户手动解决 SSH/HTTPS 连接问题

项目已经具备了基本的小说阅读功能，可以进行测试和使用。下一步建议优先解决 Git 推送问题，然后实现阅读进度的持久化存储。
